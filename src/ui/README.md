# Demo Gateway Auth UI

A minimal React + TypeScript frontend created with Vite.

## Available scripts

```bash
npm install
npm run dev
npm run build
npm run preview
```

## Notes

- The dev server runs on port `5173` by default.

